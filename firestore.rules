rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all by default — buka per-collection di phase berikutnya (PRD §7.1)
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
